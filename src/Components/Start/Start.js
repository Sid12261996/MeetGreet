import React, { Component } from 'react'

export default class Start extends Component {
    render() {
        return (
            <div>
                <h1>Inside</h1>
                <button>Logout</button>
            </div>
        )
    }
}
